.form-horizontal
  =form_for @dog do |f|
    .form-group
      =f.label :name
      =f.text_field :name
    .form-group
      =f.label :breed
      =f.text_field :breed
    .form-group
      =f.submit class: 'btn btn-primary'
